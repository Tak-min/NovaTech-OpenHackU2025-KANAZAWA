# ThinkWay

作成日: 2026-05-22

## 判断ログ

- ヘッダー/フッターの雲画像はSoraLogらしさなので削除せず、表示高と重なりを抑えて実コンテンツ領域を広げる。
- ログイン後の画面は文字・画像が小さく見えるため、ホーム/地図/ランキング/設定の主要要素を相対的に拡大し、余白を詰めすぎない。
- 位置情報は「地図の表示開始地点」と「サーバーへ送信する位置ログ」を分ける。ページ表示時の現在地取得は地図表示のために行い、設定ON時の送信ロジックは既存のまま守る。
- ピンが見えない問題は、画像アセット読み込みや公開ユーザー不足の影響を受けるため、LeafletのCSS/HTMLベースの丸ピンを標準にして視認性を安定させる。
- ダミーデータ30件はDBやAPIを汚さず、フロントの地図表示用デモデータとして追加する。名前は自然な表示名にし、日本各地に散らす。
- ランキング読み込みはテーブル内表示だけでなく、ユーザーに気づきやすいトースト/ポップアップで明示する。
- 実装範囲は主に `frontend` に限定し、既存API仕様・認証・設定保存・位置情報送信は変更しない。

## 追加判断ログ

- ランキング読み込みポップアップは、API応答が速い環境だと一瞬で消えてユーザーに伝わらないため、最低表示時間を持たせて「読み込んでいる」状態が目に入るようにした。
- 画面切り替え時に前画面のスクロール位置が残ると、スマホアプリとしての一貫性が落ちるため、ページ表示時にメイン領域のスクロールを先頭へ戻す。
- 現在地取得は地図表示の補助として扱い、設定ON時のサーバー送信ロジックとは分ける。これにより、ユーザーに現在地のピンを見せつつ、既存の同意ベースの位置ログ送信は壊さない。
- ダミーデータはDBへ投入せず、地図表示用のフロントエンド内サンプルとして追加する。展示時の見栄えを改善しながら、本番データやAPI仕様へ副作用を出さないため。
- フッターナビのクリック処理と `showPage` の両方で地図・ランキング読み込みが走っていたため、画面ごとの副作用は `showPage` に寄せる。ランキングAPIの二重呼び出しとポップアップのちらつきを抑えるため。
- 地図ピンは視覚上だけでなくDOM上でも種類を判別できるように `data-marker-type` と `aria-label` を付ける。今後のテストやアクセシビリティ改善に使いやすくするため。
- ホーム更新も `showPage('page-home')` に寄せ、ログイン復元時・ログイン成功時の重複 `updateHomePageStatus()` を外す。API負荷と描画の揺れを減らすため。
